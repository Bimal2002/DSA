// Let’s flatten all elements of the matrix into a 1D array.

// Let arr = all N*N elements of matrix

// Sort it.

// Then:

// The maximum upper triangle sum = sum of the largest N(N+1)/2 elements.

// The minimum upper triangle sum = sum of the smallest N(N+1)/2 elements.

// Because we can rearrange freely (row/col swaps give full positional freedom).
